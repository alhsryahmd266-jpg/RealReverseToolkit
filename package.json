{
  "name": "real-reverse-toolkit",
  "version": "1.0.0",
  "description": "Real Reverse Engineering Toolkit with Termux Integration",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "build:android": "npm run build && npx cap sync android && cd android && chmod +x gradlew && ./gradlew assembleDebug"
  },
  "dependencies": {},
  "devDependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/cli": "^6.0.0",
    "@capacitor/core": "^6.0.0",
    "vite": "^5.0.0"
  }
}